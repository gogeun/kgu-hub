{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Chrome에서 index.html 열기",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}